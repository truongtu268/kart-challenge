apiVersion: v1
kind: ConfigMap
metadata:
  name: product-service-config
  namespace: kart-challenge
data:
  DATABASE_URL: "postgres://productuser:productpass@postgres:5432/product_db?sslmode=disable"
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "8080"
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: kart-challenge
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
      - name: product-service
        image: product-service:latest
        imagePullPolicy: Never  # For Kind local images
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: product-service-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      initContainers:
      - name: wait-for-postgres
        image: postgres:15-alpine
        command:
        - sh
        - -c
        - |
          until pg_isready -h postgres -p 5432 -U productuser; do
            echo "Waiting for postgres..."
            sleep 2
          done
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: kart-challenge
spec:
  selector:
    app: product-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP